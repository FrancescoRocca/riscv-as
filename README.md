# Assembler

Small assembler for our RISC-V core.

## Building

```bash
$ meson setup build
$ cd build
$ meson compile
```

### Definitions schema

Where you find 0xFF it means that it doesn't have a func7 field.
